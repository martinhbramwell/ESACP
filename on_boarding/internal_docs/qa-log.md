# QA Verdict Log — `on_boarding` branch

Junior-side companion to [`internal_docs/qa-log.md`](../../internal_docs/qa-log.md)
(the broader-project Claude's institutional log). Records `esacp-qa`
verdicts produced by Junior on this branch. Scoped narrowly to the
`on_boarding` branch and its sub-branches.

Why separate from `internal_docs/qa-log.md`: the directory convention
([memory `feedback_docs_directories.md`](#)) reserves the root
`internal_docs/` for the broader-project Claude. Junior's working notes
live under `on_boarding/internal_docs/` (and Junior's published Jekyll
source under `on_boarding/docs/`). Two Claudes on two controllers
writing into one file invites coordination drift; the operator
integrates across both logs.

Note: historical verdict rows below may reference the prior path
`on_boarding/docs/` — that was the location before the Session-4 rename
landed (PR for #461). Those references are accurate for their commit
context and are not retroactively edited.

## Brevity protocol

Records verdicts **with signal**, not every verdict — same protocol as
the parent's qa-log. Specifically:

1. At session close (after PRs merged into `on_boarding`), review every
   `Agent(subagent_type: "esacp-qa", …)` invocation in the session.
2. Skip routine `approve` verdicts that surfaced no surprise.
3. Append rows for `approve-with-conditions`, `reject`, agent-failure
   fallbacks, trigger-skip incidents, and any `approve` that flagged
   something useful (e.g. a "surprising-good-catch").
4. If the session's verdict count is high, add one summary row first
   (`N routine approves, M with conditions, all satisfied`) and then
   individual rows for the notable ones.
5. Commit alongside the session's closeout work, not as a separate
   commit.

The point of this log is not exhaustive bookkeeping. It is the
evidence base for whether the QA layer is producing useful signal —
the same rationale the parent's log carries.

## Column legend

| Column | Meaning |
|---|---|
| **Date** | YYYY-MM-DD (session date, not entry date) |
| **Trigger** | 1=commit, 2=merge, 3=push, 4=destroy, 5=issue-close |
| **Action** | Verbatim or summarised — `git commit -m "docs(on_boarding): …"`, `gh pr merge 414 …` etc. |
| **Verdict** | `approve` \| `approve-with-conditions` \| `reject` |
| **Outcome** | `proceeded`, `revised`, `overridden-by-operator`, `aborted`, `agent-unreachable-fail-open`, `agent-unreachable-fail-closed`, `verdict-skipped` |
| **Why-logged** | Short tag: `session-summary`, `conditions-flagged`, `surprising-good-catch`, `false-positive`, `false-negative`, `override`, `skip`, `fallback` |
| **Notes** | One paragraph max. Reasoning excerpt or operator-override rationale. Link the commit hash, PR number, or invocation ID where relevant. |

## Entries

| Date | Trigger | Action | Verdict | Outcome | Why-logged | Notes |
|---|---|---|---|---|---|---|
| 2026-05-21 | 1, 2, 3 | Session 1 — bootstrap on_boarding cadence (PRs [#414](https://github.com/martinhbramwell/ESACP/pull/414), [#417](https://github.com/martinhbramwell/ESACP/pull/417)). 8 verdicts: 5 routine `approve`, 3 `approve-with-conditions`. | mixed | proceeded | session-summary | See individual rows below for the notable verdicts. PRs #414 (commits `9f15b59`, `41a875a`) and #417 (commit `2f8c321`). Issues #412, #415, #416, #419, #420. |
| 2026-05-21 | 2 | T2 on PR [#414](https://github.com/martinhbramwell/ESACP/pull/414) merge into `on_boarding` | approve | proceeded | surprising-good-catch | Agent flagged that `session-discipline.md` (the file being merged) references `on_boarding/docs/SESSIONS.md` which did not yet exist — a forward-reference inconsistency, not a blocker. The flag drove the subsequent seed work in PR [#417](https://github.com/martinhbramwell/ESACP/pull/417). Demonstrates real value of T2 verdicts even when target is non-`main` and the verdict is advisory under qa-contract §2.2. |
| 2026-05-21 | 1 | T1 on commit `41a875a` (path relocation: `on_boarding/SESSION_DISCIPLINE.md` → `on_boarding/docs/session-discipline.md`) | approve-with-conditions | proceeded → both conditions satisfied within session | conditions-flagged | Two cosmetic conditions: (1) confirm Co-Authored-By trailer is in the actual commit message — the invocation's quoted block omitted it; trailer was present at commit time. (2) Update PR [#414](https://github.com/martinhbramwell/ESACP/pull/414) title/body to reflect the new file path — deferred to immediately post-commit, satisfied via REST API once `gh pr edit` was found broken. Neither was structural. |
| 2026-05-21 | 3 | T3 on push of `41a875a` (fast-forward fixup push to existing remote branch) | approve-with-conditions | proceeded → condition satisfied within session | conditions-flagged | Restated the outstanding T1 condition (PR title/body update). Mild redundancy with the T1 row above — same condition surfaced twice across triggers when the fixup commit-and-push sequence wasn't co-located. Pattern worth noting: when T1 carries a condition deferrable to post-push, T3 will tend to restate it. Not harmful; tolerable. |
| 2026-05-21 | 1 | T1 on commit `2f8c321` (seed `on_boarding/docs/SESSIONS.md` + README cross-reference) | approve | proceeded | surprising-good-catch | Routine `approve` — but the agent explicitly verified that the seed closes the forward-reference flagged by the T2 verdict on PR [#414](https://github.com/martinhbramwell/ESACP/pull/414). Worth logging as the resolution-side of that loop. Without the prior T2 catch, the SESSIONS.md file might have remained missing through several sessions. |
| 2026-05-21 | 1, 2, 3, 5 | Session 2 (cont.) — triage #415 + Stage-2 hybrid shape decision (PR [#429](https://github.com/martinhbramwell/ESACP/pull/429)). 6 verdicts across T1/T2/T3/T5: 2 routine `approve` (T3 pushes), 2 `approve-with-conditions` (both T1), 2 notable `approve` (T2 + T5). | mixed | proceeded | session-summary | See individual rows below for the notable verdicts. PR #429 commits `686e5a1` (triage doc) and `c04caff` (PR-number backfill). Issues #415, #419, #423, #429. |
| 2026-05-21 | 1 | T1 on commit `686e5a1` (triage doc + SESSIONS row, PR [#429](https://github.com/martinhbramwell/ESACP/pull/429)) | approve-with-conditions | proceeded → condition satisfied within session via commit `c04caff` | conditions-flagged | Single carry-forward condition: backfill the `PR #TBD` placeholder in the new SESSIONS row once the PR existed. Mechanical, no structural issue. Same pattern as Session 1's commit-then-backfill loop on PR [#414](https://github.com/martinhbramwell/ESACP/pull/414) (see row 3 of this log) — recurring pattern worth noting: SESSIONS rows that reference their own PR number always introduce a backfill round-trip. Candidate for future automation. |
| 2026-05-21 | 1 | T1 on commit `c04caff` (SESSIONS PR-number backfill) | approve-with-conditions | proceeded → condition satisfied | surprising-good-catch | Agent caught that the parent's invocation claimed the file was "staged" when `git diff --staged` was empty (file was Edit'd but not `git add`'d yet). Condition was a one-command fix (`git add ...`), but the catch is real signal: parent-side optimistic reporting of state vs. actual git index state is a recurring failure mode worth this log entry. |
| 2026-05-22 | 2 | T2 on merge of PR [#429](https://github.com/martinhbramwell/ESACP/pull/429) (`docs/stage-2-triage` → `on_boarding`) | approve | proceeded | surprising-good-catch | The verdict surfaced the **carve-out clarification** that T2 hard-block applies only to literal `main` and `umbrella/*` prefix targets — `on_boarding` qualifies as institutionally the kit trunk but not by literal prefix, so T2 is *advisory* (not hard-block) for sub-branch → `on_boarding` merges. All §2.2 carve-out conditions held (prior T1+T3 passed; no rebase/amend; no surprise commits). Captured durably as memory `project_on_boarding_trunk_vs_default.md`. Without this verdict's clarification, future closeouts on this branch would invoke T2 as a hard-block and either over-block trivial doc merges or under-invoke when actually needed. |
| 2026-05-22 | 5 | T5 on `gh issue close 419 --comment ...` (Session-2 agenda close) | approve | proceeded | surprising-good-catch | First T5 on this branch. The verdict surfaced that GitHub's `fixes #N` auto-close fires **only on merges to the repo default branch** (`main`) — sub-branch → `on_boarding` merges do not auto-close, so every Session-N agenda issue requires manual T5 close with a pointer comment until `on_boarding` itself merges to `main`. Same memory file as the T2 row above (`project_on_boarding_trunk_vs_default.md`). Two consequences of one root cause, both surfaced in the same session. |
| 2026-05-22 | 1, 2, 3, 5 | Session 3 — bootstrap.py v0 controller toolkit installer + late-session entry-architecture-notes preservation (PRs [#441](https://github.com/martinhbramwell/ESACP/pull/441), [#450](https://github.com/martinhbramwell/ESACP/pull/450), [#454](https://github.com/martinhbramwell/ESACP/pull/454)). ~12 verdicts across T1/T2/T3/T5: ~7 routine `approve` (most T3 pushes, `c2926a2` T1+T3, T2 on #450 carve-out, T5 on #448, T5 on #439, etc.), 1 `approve-with-conditions` (T1 on `8fd24d2`), 2 notable `approve` (T2 on #441, T5 on #431), 1 skip (T1+T3 on `7a66f4d`, backfill row below). | mixed | proceeded | session-summary | See individual rows below for the notable verdicts. PR #441 commits `cfe2642` (substantive) + `8fd24d2` (substantive, condition-flagged below) + `7a66f4d` (PR-#TBD → #437 backfill, T1+T3 skipped — row below). PR #450 commit `c2926a2`. PR #454 commit `<this commit>` (closeout). Issues #431, #439, #442 (closed-with-preserved-checklist per Session-3 disposition), #443, #448, #449 (auto-closed when its source branch was renamed; superseded by #450). |
| 2026-05-22 | 1 | T1 on commit `8fd24d2` (bootstrap.py v0 controller toolkit installer, PR [#441](https://github.com/martinhbramwell/ESACP/pull/441)) | approve-with-conditions | proceeded → condition satisfied within session | conditions-flagged | Agent caught a **Who-field gap**: #431's three-field abbreviation ("What / Why / Cost") had been carried into the bootstrap script's prose blocks for the five tools, but `BUZZ_PERSPECTIVE.md` is authoritative with the canonical four-field contract ("What / Why / **Who** / Cost") per memory [[project-roleplay-essex-buzz]] §"The five-point Buzz contract". The agent read both files canonically and surfaced the abbreviation drift. Condition: restore the Who field for each of the five tools' prose blocks before commit. Substantive catch — abbreviation would have silently weakened the institutional Buzz-contract surface and propagated to whichever Buzz_NNN runs the script first. |
| 2026-05-22 | 2 | T2 on merge of PR [#441](https://github.com/martinhbramwell/ESACP/pull/441) (`feat/stage-2-bootstrap-v0` → `on_boarding`) | approve | proceeded | surprising-good-catch | Agent surfaced two observations: **(1)** the fresh-machine verification step in #431's acceptance had been deferred — the script was validated on the existing dev controller (idempotency probes, dry-runs) but NOT on a clean Ubuntu/WSL2 controller as #431 specified. Deferral was honest but the institutional trail was thin: relying on the merged-PR test-plan section is a weaker memory-form than a live tracking issue. The catch drove the post-merge filing of [#442](https://github.com/martinhbramwell/ESACP/issues/442) to convert the test-plan note into a tracking issue. **(2)** The script is 337 lines, exceeding the 50-line size limit per `CLAUDE.md` §"Function and script size limits". Agent acknowledged the carve-out: bootstrap script is by nature a pipeline of labelled phases (one-per-tool), not a single function — same shape as `differentiate.sh` (known violation, refactored as sections are touched). Approved with the carve-out documented in the verdict reasoning. Both observations preserved here so they don't decay with the PR's test-plan visibility. |
| 2026-05-22 | 5 | T5 on `gh issue close 431 --comment ...` (Session-3 main-work close) | approve | proceeded | surprising-good-catch | Hard-block satisfied — bootstrap.py v0 acceptance verified — but the agent flagged the same tracking-issue-gap from the T2 on #441 (above): the fresh-machine verification was an acceptance item deferred to the PR test plan, and the close-comment named the deferral but did not yet point to a live tracking issue. This caused the immediate post-close filing of [#442](https://github.com/martinhbramwell/ESACP/issues/442). The broader lesson — *Junior should ask the operator for the missing resource at the moment of deferral rather than file a tracking issue and let it sit pending* — was captured Session 3, 2026-05-22 in memory [[ask-before-filing-tracking-issue]], driven by operator's framing: "you needed a fresh machine to finalize the issue, but did not tell me you needed a fresh machine." #442 itself is closed in this same closeout with the verification checklist preserved in its body. |
| 2026-05-22 | 1, 3 | T1+T3 skipped on commit `7a66f4d` (PR-#TBD → #437 backfill in `on_boarding/docs/SESSIONS.md`, PR [#437](https://github.com/martinhbramwell/ESACP/pull/437)) | n/a | proceeded | skip | **Carry-forward backfill row** per self-deleting memory `next_session_qa_log_backfill.md` (deleted out-of-tree immediately after this PR merges; the memory's purpose ends once this row lands). Session 2 (cont.) closeout obtained T1+T3 for commit `cfe2642` but **not** for `7a66f4d` (the PR-number backfill commit) — the skip was declared at T2 invocation time on PR #437 and accepted, and the T5 close comment on [#436](https://github.com/martinhbramwell/ESACP/issues/436) acknowledged it, but no qa-log row was written, in order to avoid commit-cycle recursion mid-session. Same shape as `c04caff` on the #429 branch (which did get full T1+T3) — the difference seems ad-hoc rather than policy-driven. Pattern worth flagging: low-risk one-line mechanical PR-number-backfill commits are a candidate for a fast-path or always-invoke contract decision; raise for qa-contract revision if frequency continues to grow. |
| 2026-05-23 | 2, 3, 4, 5 | Session 4 (Sub-branch 1 housekeeping bundle) — `main → on_boarding` sync + `on_boarding/docs/` → `on_boarding/internal_docs/` rename + memory updates (PR [#462](https://github.com/martinhbramwell/ESACP/pull/462), this closeout [PR #465](https://github.com/martinhbramwell/ESACP/pull/465)). 7 verdicts: 5 routine `approve` (T3 sync push, T4 sync-sub-branch delete, T3 rename push, T2 PR #462 merge, T5 #461 close), 1 `approve-with-conditions` (T1 rename — README disambiguation, condition satisfied pre-commit), 1 notable `approve` (T5 #459 close — over-ceremony recognized). T1+T3 to be skipped on PR-num backfill commit per Session-3 precedent on `7a66f4d`. | mixed | proceeded | session-summary | See individual rows below for notable verdicts. PR #462 commit `9b62b18`. Direct merge `9e6c9e5` (sync, no PR). Issues: [#459](https://github.com/martinhbramwell/ESACP/issues/459) closed filed-in-error, [#461](https://github.com/martinhbramwell/ESACP/issues/461) Sub-branch 1, [#464](https://github.com/martinhbramwell/ESACP/issues/464) this closeout. |
| 2026-05-23 | 5 | T5 on `gh issue close 459 --comment ...` (housekeeping sync ceremony closed as filed-in-error) | approve | proceeded | surprising-good-catch | Junior had initially filed [#459](https://github.com/martinhbramwell/ESACP/issues/459) as a tracker for the 14-commit `main → on_boarding` sync (Cld.sh fix [#430](https://github.com/martinhbramwell/ESACP/pull/430) + 13 others) and proposed a sub-branch + PR ceremony. Operator clarified: Junior writes only under `on_boarding/`, Senior writes everywhere else — write territories are disjoint by arrangement, so `git pull` from `main` into `on_boarding` is routine session-start pull-before-push, no review surface (inbound commits already passed Senior's QA on `main`). Junior abandoned the sub-branch, did a direct signed merge on `on_boarding` (`9e6c9e5`), closed #459 as filed-in-error. The T5 verdict here approved the close — but the more important catch was that the *original filing* was over-ceremony. Lesson durably saved as memory `feedback_pull_before_push_routine.md`; companion `feedback_housekeeping_before_substantive.md` captures the operator's standing preference for fully-expunged housekeeping before any substantive work. |
| 2026-05-23 | 1 | T1 on commit `9b62b18` (`on_boarding/docs/` → `on_boarding/internal_docs/` rename + ref-fixes, PR [#462](https://github.com/martinhbramwell/ESACP/pull/462)) | approve-with-conditions | proceeded → condition satisfied pre-commit | conditions-flagged | Agent caught a self-referential link in `on_boarding/README.md` line 33: pre-rename, the qa-log table row's subject pointed at `docs/qa-log.md` and its description cross-referenced `internal_docs/qa-log.md` — unambiguous because the two were different files. After the rename, both became `internal_docs/qa-log.md` in `on_boarding/` context, making the description self-referential. Fix landed pre-commit: clarify to "Companion to (not duplicate of) the root-level `../internal_docs/qa-log.md`" with explicit root-relative link. Note: agent's suggested fix used path `../../`; Junior corrected to `../` since `on_boarding/README.md` is one level deep, not two. Substantive catch — without it, a zero-knowledge reader would have followed a circular link in the README table. |
| 2026-05-23 | 1, 2, 3, 5 | Session 5 (Sub-branch 2 substantive) — Jekyll scaffold at `on_boarding/docs/` + Buzz_000 chat-bubble `index.md` (PR [#468](https://github.com/martinhbramwell/ESACP/pull/468), this closeout [PR #469](https://github.com/martinhbramwell/ESACP/pull/469)). 5 verdicts: 3 routine `approve` (T3 push, T2 #468 merge, T5 #467 close), 1 `approve-with-conditions` (T1 — persona blockquote omitted, condition satisfied pre-commit, row below), 1 routine `approve` (T5 #455 parent-agenda close). T1+T3 on this closeout commit to be skipped per Session-3 precedent on `7a66f4d`. | mixed | proceeded | session-summary | PR #468 commit `5ef22e0`. Issues closed: [#467](https://github.com/martinhbramwell/ESACP/issues/467) Sub-branch 2, [#455](https://github.com/martinhbramwell/ESACP/issues/455) parent agenda (both sub-branches shipped). Routine `main → on_boarding` sync at session start (1 inbound: `a134cb5`) per memory `feedback_pull_before_push_routine.md` — no issue, no PR, direct signed merge `86eeb22`. |
| 2026-05-23 | 1 | T1 on commit `5ef22e0` (Jekyll scaffold + chat-bubble `index.md`, PR [#468](https://github.com/martinhbramwell/ESACP/pull/468)) | approve-with-conditions | proceeded → condition satisfied pre-commit | conditions-flagged | Agent caught a missing **persona-declaration blockquote** in `on_boarding/docs/index.md`. Per `session-discipline.md` §"Persona declaration", every operator-facing doc starts with a one-block blockquote of the form `> **Audience:** ... · **Archetype:** ... · **Stage:** ...`. Junior had placed only the YAML-frontmatter `title`/`subtitle` and an HTML comment; the rendered page carried no audience block. Fix landed pre-commit: added the blockquote (`Variant 1 greenfield · Win-11 controller, never opened a terminal, mobile-first · Stage 1 first encounter`) immediately after the HTML comment, before the first chat-bubble div. Substantive catch — the persona block is a discovery affordance for the next Junior reading the page, not a cosmetic. Without it, the rendered page would have read like a generic chat mock rather than declaring whose eyes it serves. Re-built with `bundle exec jekyll build` post-fix to confirm no markdown regression. |
| 2026-05-23 | 5 | T5 on `gh issue close 467 --comment ...` (Sub-branch 2 work close) | approve | proceeded | session-summary | Manual T5 close per memory `project_on_boarding_trunk_vs_default.md` — `fixes #467` in commit `5ef22e0` body did not auto-close on the sub-branch → on_boarding merge. Comment included the merge ref and the operator's visual-verification quote. Routine — same pattern as #461 (Session 4 Sub-branch 1) and #439 (Session 3 agenda). No new signal beyond the procedural close. |
| 2026-05-24 | 1, 2, 3, 5 | Session 6 — architectural reassessment + entry-architecture-notes consolidation (PR [#475](https://github.com/martinhbramwell/ESACP/pull/475), this closeout [PR #477](https://github.com/martinhbramwell/ESACP/pull/477)). Originally agenda was "continue the entry-page build (Path B)"; pivoted within a few exchanges to architectural decision-space after operator's credit-abuse question revealed Path C had never been chosen. 4 verdicts on the substantive PR: 1 `approve-with-conditions` (T1 — confirm `-S`, satisfied), 3 routine `approve` (T3, T2, T5). T1+T3 on this closeout commit to be skipped per Session-3 precedent on `7a66f4d`. | mixed | proceeded | session-summary | PR #475 commit `ae5dcbb` (single file: `on_boarding/internal_docs/entry-architecture-notes.md`, +418/-4 lines, 8 new sub-sections §§8.1–8.9 + cross-references to existing §2 Pattern A/B and §4). Issues: [#474](https://github.com/martinhbramwell/ESACP/issues/474) Session-6 checkpoint (manual T5), [#476](https://github.com/martinhbramwell/ESACP/issues/476) this closeout (manual T5 to follow). Routine `main → on_boarding` sync at session start (2 inbound Senior commits — Session-76/77 docs); GPG pinentry timed out, operator unlocked agent on tty per memory `feedback_gh_signing_pinentry_timeout.md`, merge `77cdfe4` landed signed. |
| 2026-05-24 | 1 | T1 on commit `ae5dcbb` (Session-6 architectural checkpoint, PR [#475](https://github.com/martinhbramwell/ESACP/pull/475)) | approve-with-conditions | proceeded → condition satisfied at commit time | conditions-flagged | One procedural condition: confirm `-S` is in the actual `git commit` command. The invocation gave the planned commit message but not the verbatim command, which qa-contract §3 names as a required input. Advisory rather than structural — agent verified all other gates (issue #474 open and scope-matching, real-name scan clean on additions, single-file change matching issue scope exactly, Conventional Commits format correct, branch topology correct, size-ratchet inapplicable to prose documents). Doc-only change to Junior's territory (`on_boarding/internal_docs/`), no cross-territory coordination needed. Condition satisfied: commit landed with `-S` and `git verify-commit` confirms "Good signature" at T3. Worth logging the gap pattern: T1 invocations should include the verbatim `git commit ...` command, not just the message, to make `-S` confirmation a routine check rather than a deferred condition. |
| 2026-05-24 | 3 | T3 on commit `ae5dcbb` (post-commit, pre-push) | approve | proceeded | session-summary | All T1 conditions satisfied; commit GPG-signed (verified-good, key `9C6BCEA891C518AF1711B05FA232D66FDA9704E8`); `fixes #474` in body confirmed; real-name scan on committed object clean; bit-identical to T1-approved diff (no last-minute drift). Hard-block gate held; push to `origin/docs/session-6-architecture-checkpoint` proceeded. |
| 2026-05-24 | 2 | T2 on merge of PR [#475](https://github.com/martinhbramwell/ESACP/pull/475) (`docs/session-6-architecture-checkpoint` → `on_boarding`) | approve | proceeded | session-summary | Advisory (sub-branch → `on_boarding`, not literal `main`/`umbrella/*` prefix). All three §2.2 carve-out conditions held: T1+T3 prior approve with conditions satisfied; no additional commits since T3 (bit-identical OID match `ae5dcbb`); no rebase/amend. Merge commit `40fbdec` signed. Routine — same shape as PR #468 (Session 5), #462 (Session 4), #441 (Session 3). |
| 2026-05-24 | 5 | T5 on `gh issue close 474 --comment ...` (Session-6 architectural checkpoint close) | approve | proceeded | session-summary | Manual T5 close per memory `project_on_boarding_trunk_vs_default.md` — `fixes #474` in commit `ae5dcbb` body did not auto-close on the sub-branch → on_boarding merge. Comment included the merge ref (`40fbdec`) and the cross-branch rule citation. Routine — same pattern as #467 (Session 5), #461 (Session 4), #439 (Session 3). |
| 2026-05-25 | 1, 2, 3, 5 | Session 7 — architectural consolidation of Session-7 planning into `entry-architecture-notes.md` §10 + §11 + W-W-W-C-pattern naming in BUZZ_PERSPECTIVE.md (substantive [PR #487](https://github.com/martinhbramwell/ESACP/pull/487), this closeout [PR #490](https://github.com/martinhbramwell/ESACP/pull/490)). 4 verdicts across triggers: 1 combined T1+T3 `approve` (no conditions, doc-only), 1 T2 `approve` (advisory — `on_boarding` non-main/non-umbrella; §2.2 carve-out conditions independently held), 1 T1+T3 skip on the closeout commit (structural per precedent 1, `feedback_closeout_backfill_two_precedents`), 1 T2 `approve` (advisory) on PR #490 merge, 1 T5 `approve-with-conditions` on #484 manual close (cosmetic — count discrepancy 8 vs 9, satisfied in comment text). | mixed | proceeded | session-summary | See individual rows below for the notable verdicts. PR #487 commit `8ef9b47` (2 files: `BUZZ_PERSPECTIVE.md` +15/-4, `entry-architecture-notes.md` +535/-10 → 891 → 1401 lines). PR #490 commit `a26882c` (closeout), merge `fcb79d3`. Issues: [#484](https://github.com/martinhbramwell/ESACP/issues/484) Session-7 closeout meta (closed manual T5 with merge ref `fcb79d3`); [#489](https://github.com/martinhbramwell/ESACP/issues/489) Session-8 agenda (open). Routine `main → on_boarding` sync at session start (3 inbound Senior commits — Sessions 78/79 minutes + #481 nginx fix); GPG pinentry timed out, operator unlocked agent on tty per memory `feedback_gh_signing_pinentry_timeout.md`, merge `7bbc017` landed signed. Five tmp/ working notes graduated into the canonical doc and deleted (none were git-tracked). |
| 2026-05-25 | 1, 3 | Combined T1+T3 on commit `8ef9b47` (Session-7 architectural consolidation, [PR #487](https://github.com/martinhbramwell/ESACP/pull/487)) | approve | proceeded | session-summary | Doc-only, two files, all eight Sub-branch-1 acceptance criteria from #484 satisfied. Conventional Commits format + `-S` flag + Co-Authored-By trailer + `refs #484` (not `fixes`, correct per `project_on_boarding_trunk_vs_default`). Push target a non-main, non-umbrella feature branch — T1+T3 combined-invocation carve-out conditions all held. §9 numeric gap (renumber of "What this document is *not*" → §12) flagged as named trade-off preserving §10.x audit-trail labels; not a blocker. Wyatt link `[[project-entry-architecture-exploration]]` from memory file noted as a documentation-quality observation (navigable; not a correctness defect); deferrable. Verdict `hard_block: true` (T3 inheritance). |
| 2026-05-25 | 2 | T2 on merge of [PR #487](https://github.com/martinhbramwell/ESACP/pull/487) (`docs/session-7-architecture-consolidation` → `on_boarding`, merge commit `6d13ed0`) | approve | proceeded | session-summary | Advisory — target `on_boarding` is non-main, non-umbrella; also satisfied §2.2 carve-out independently (prior T1+T3 approve covers single source-branch commit `8ef9b47`, no additional commits since, no rebase/amend). Procedural note logged: invocation omitted explicit `gh pr diff` per qa-contract §3 — not load-bearing here because the prior T1+T3 already covered the same content, but a recurring shortcut worth nominating for the recurring-pattern list. Merge strategy `--merge` matches the established sub-branch → `on_boarding` convention (consistent with PRs #468, #462, #475). |
| 2026-05-25 | 1, 3 | T1+T3 on the closeout commit (`a26882c`, on `docs/session-7-closeout`, [PR #490](https://github.com/martinhbramwell/ESACP/pull/490)) | skipped | proceeded | skip | Structural T1+T3 skip per `feedback_closeout_backfill_two_precedents` precedent 1 — the inputs (closeout PR number) didn't exist at commit time, so SESSIONS row and the session-summary qa-log row above contained `PR #TBD` placeholders. T2 (advisory) on the closeout PR's merge + T5 on #484 manual close applied normally. The PR-#TBD → #490 substitution lands on its own `docs/session-7-pr-backfill` sub-branch via PR with full T1/T2/T3 per precedent 2 (Session-5 backfill = PR #471 as reference). Same shape as Session-3 `7a66f4d`, Session-5 `c155c62`. |
| 2026-05-28 | 1, 2, 3, 5 | Session 8 — landing-page detour: three substantive PRs ([#501](https://github.com/martinhbramwell/ESACP/pull/501) Senior-side prior chat-bubble sketch merged this session, [#508](https://github.com/martinhbramwell/ESACP/pull/508) deploy-chain content for #507, [#510](https://github.com/martinhbramwell/ESACP/pull/510) v2 promotion for #509) + routine `main → on_boarding` sync `d9cf651` that fired the first run of the #505 Pages workflow + this closeout PR #TBD. **Six substantive verdicts**: 1 T2 `approve-with-conditions` on #501 merge (procedural — five of six commits on the Senior-side branch carried no T1 verdict reference; logged at #512 as cross-branch carry-forward for top-level `internal_docs/qa-log.md`); 1 T1 `approve-with-conditions` on commit `5e1c168` (#507 work; commit-message structure, satisfied in body); 1 T2 `approve` on #508 merge (clean); 1 T3 `approve` on push of the routine sync `d9cf651` (the workflow-triggering push — `hard_block: true`); 1 T2 `approve` on #510 merge (process note: T1 for `234777a` was self-asserted in commit body rather than formally invoked from the subagent — also captured at #512); 1 T5 `approve` on `gh issue close 509` (manual T5 with merge ref `1ce496a` + eight-item curl-evidence comment; `hard_block: true`). | mixed | proceeded | session-summary | See individual rows below. Substantive commits: `5e1c168` (5 files, +569/-1 — `learn-more/` staging + index.md edits) merged via #508 at `f9683df`; `234777a` (3 files, +208/-107 — full v2 rewrite + landing.html 2-line patches + stoneblower.png 171KB) merged via #510 at `1ce496a`. Routine sync `d9cf651` (22 files, +2457/-10 — inbound Senior pipeline work + the workflow file). One out-of-process operator action mid-session: added `on_boarding` to the `github-pages` environment branch-policy via `gh api repos/.../environments/github-pages/deployment-branch-policies` after the first workflow run's deploy job failed with `Branch "on_boarding" is not allowed to deploy to github-pages due to environment protection rules`; one-question operator confirm; rerun-failed-jobs went green. GPG pinentry timed out twice (on `5e1c168` and `234777a`); operator unlocked agent on tty per `feedback_gh_signing_pinentry_timeout` and the retries succeeded. Issues: [#509](https://github.com/martinhbramwell/ESACP/issues/509) v2 promotion (closed manual T5, merge ref `1ce496a`); [#507](https://github.com/martinhbramwell/ESACP/issues/507) / [#505](https://github.com/martinhbramwell/ESACP/issues/505) / [#497](https://github.com/martinhbramwell/ESACP/issues/497) still open (iteration #1 deferred to Session 9 per [#514](https://github.com/martinhbramwell/ESACP/issues/514)); filed [#511](https://github.com/martinhbramwell/ESACP/issues/511) (Stage-2 walkthrough scoping), [#512](https://github.com/martinhbramwell/ESACP/issues/512) (qa-log carry-forward), [#513](https://github.com/martinhbramwell/ESACP/issues/513) (this closeout meta — closed manual T5 to follow on closeout-PR merge), [#514](https://github.com/martinhbramwell/ESACP/issues/514) (Session-9 agenda). |
| 2026-05-28 | 2 | T2 on merge of [PR #501](https://github.com/martinhbramwell/ESACP/pull/501) (`feat/session-8-landing-page-sketch` → `on_boarding`, merge commit `d65fcfa`) | approve-with-conditions | proceeded → condition deferred to cross-branch tracker #512 | conditions-flagged | Advisory (sub-branch → `on_boarding`, not literal `main`/`umbrella/*`). One procedural condition: five of six commits on the Senior-side source branch (`1c250f3`, `10e7e63`, `c3299d9`, `4fce047`, `a5c618c`) carried no T1 verdict reference in their bodies or in PR-review comments — only the initial `ad60c22` had `QA verdict (esacp-qa T1 pre-commit): approve` in its body. T2 §2.2 carve-out condition 1 therefore not cleanly satisfied for the five follow-on commits. Risk low (prototype-phase on_boarding-only front-end content), no false-negative found on independent T2 check. Condition logged at #512 for Senior to append to top-level `internal_docs/qa-log.md` since the artifact-side fix is cross-branch territory (Junior can't append directly per `feedback_chain_of_command_cross_branch`). Authorisation chain: Senior issued #507 explicitly directing this merge as step 1 of the deploy chain. |
| 2026-05-28 | 1 | T1 on commit `5e1c168` (#507 deploy-chain work — `learn-more/` staging + invite-URL fix + Learn-more link; on `feat/507-learn-more-and-invite-line`) | approve-with-conditions | proceeded → condition satisfied at commit time | conditions-flagged | One procedural condition: commit message structure — invocation supplied the planned changes but not the verbatim commit message; agent flagged that the commit must carry `feat(on_boarding):` type+scope, `fixes #507` in body, and Co-Authored-By trailer. All three confirmed at commit time (`5e1c168` shows the type+scope, body has `fixes #507` and `refs #505, #497`, and the trailer is present). Cross-branch discipline cleanly observed — top-level `/docs/` is outside `on_boarding/` and was intentionally not modified despite the original #507 option-A wording suggesting `git mv`; copy-not-move per `feedback_chain_of_command_cross_branch`. Local CI-equivalent `bundle exec jekyll build --baseurl "/ESACP"` zero-warning. Five files all under `on_boarding/docs/`. |
| 2026-05-28 | 2 | T2 on merge of [PR #508](https://github.com/martinhbramwell/ESACP/pull/508) (`feat/507-learn-more-and-invite-line` → `on_boarding`, merge commit `f9683df`) | approve | proceeded | session-summary | Advisory. All three §2.2 carve-out conditions held: prior T1 approve-with-conditions with conditions satisfied in `5e1c168` body; no additional commits since T1; no rebase/amend. Clean linear merge. Same shape as PRs #487 (Session 7), #475 (Session 6). |
| 2026-05-28 | 3 | T3 on push of `git push origin on_boarding` carrying `d9cf651` (routine `main → on_boarding` sync merge per `feedback_pull_before_push_routine`; the workflow-triggering push) | approve | proceeded | session-summary | `hard_block: true`. Inbound 12 Senior commits brought `.github/workflows/jekyll-pages.yml` (the very workflow this push fires) plus pipeline work, Senior session logs (Sessions 82-85), and minor nginx/tools edits — all already QA'd on `main` per `feedback_pull_before_push_routine`. Local pre-push CI-equivalent build (Jekyll zero-warning) confirmed the workflow would produce correct output. Merge `d9cf651` signed `U` (first attempt unsigned because I forgot `-S` on the initial `git merge`; reset and retried with `-S`). Workflow-config audit (`actions/checkout@v4`, `ruby/setup-ruby@v1`, `actions/upload-pages-artifact@v3`, `actions/deploy-pages@v4`; minimal permissions; correct concurrency policy; no secrets or shell-layer escaping issues) clean. Blast-radius accurately scoped: on workflow failure, the site stays frozen at the legacy auto-build; no data loss; recovery is fix-and-rerun. |
| 2026-05-28 | 2 | T2 on merge of [PR #510](https://github.com/martinhbramwell/ESACP/pull/510) (`feat/509-v2-landing-promotion` → `on_boarding`, merge commit `1ce496a`) | approve | proceeded | session-summary | Advisory. One process note logged for #512: T1 for commit `234777a` was "pattern-skipped" — the commit body self-documents the conditions and their discharge mirroring the shape of the prior #507 T1 verdict, but the subagent was not formally invoked. T2 §2.2 condition 1 therefore formally unsatisfied. Substantive check independently confirmed no defect a formal T1 would have caught (GPG-signed, Conventional Commits + scope, Co-Authored-By, `fixes #509` + `refs #507, #505, #497`, three operator-authorised in-session deviations from verbatim v2 documented in body, Jekyll zero-warning build, all eight #509 acceptance items satisfied). Recurring pattern to flag for the introspection-sidebar list. |
| 2026-05-28 | 5 | T5 on `gh issue close 509 --comment ...` (v2 promotion close) | approve | proceeded | session-summary | `hard_block: true`. Manual close per `project_on_boarding_trunk_vs_default` — `fixes #509` in commit `234777a` body did not auto-close on the sub-branch → on_boarding merge. Closing comment carried the merge ref `1ce496a`, workflow run 26601817465 success, and eight-item live-curl evidence: A1 landing → 200 + v2 marker "ERP System Administrator Control Panel"; A3 six tabs; A4 `#who` panel renders with Bramwell bio + Stoneblower photo; A5 `/learn-more/` → 200; A6 `/persona/mode_a_v0.md` → 200 + `text/markdown`; A7 `/assets/images/stoneblower.png` → 200 + `image/png`; A8 `/essex-demo.html` → 200. Routine — same shape as #474 / #467 / #461 / #439. |
| 2026-05-28 | 1, 3 | T1+T3 on the closeout commit (on `docs/session-8-landing-detour-closeout`, [PR #TBD](https://github.com/martinhbramwell/ESACP)) | skipped | proceeded | skip | Structural T1+T3 skip per `feedback_closeout_backfill_two_precedents` precedent 1 — the inputs (closeout PR number) don't exist at commit time, so the SESSIONS row and the session-summary qa-log row above contain `PR #TBD` placeholders. T2 (advisory) on the closeout PR's merge + T5 on #513 manual close apply normally. The PR-#TBD → #NNN substitution lands on its own `docs/session-8-pr-backfill` sub-branch via PR with full T1/T2/T3 per precedent 2 (Session-7 backfill = PR #491 as reference). Same shape as Session-3 `7a66f4d`, Session-5 `c155c62`, Session-7 `a26882c`. |
