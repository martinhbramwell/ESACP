# QA Verdict Log — `esacp-qa` Subagent (#341)

Records verdicts that turned out wrong, operator overrides, agent-failure-fallbacks, and trigger-skip incidents. **Not** every verdict — only the ones with signal.

**Batched at session-close** (one append per session alongside minutes). Bounded, predictable churn.

**Why this log exists:** if recurrence rate of bad verdicts or skipped invocations matches the original "3 regressions in ~36 hours" pattern that triggered #341, escalate to v2 hook-based enforcement (PreToolUse / PreCommit hooks firing automatically on `git commit` / `gh pr merge`). Until then, this log is the evidence base for that decision.

---

## Column legend

| Column | Meaning |
|---|---|
| **Date** | YYYY-MM-DD (session date, not entry date) |
| **Trigger** | Which trigger contract item (1=commit, 2=merge, 3=push, 4=destroy, 5=issue-close) |
| **Action** | Verbatim or summarised — `git commit -m "feat(qa): …"` etc. |
| **Verdict** | `approve` \| `approve-with-conditions` \| `reject` |
| **Outcome** | What actually happened: `proceeded`, `revised`, `overridden-by-operator`, `aborted`, `agent-unreachable-fail-open`, `agent-unreachable-fail-closed`, `verdict-skipped` |
| **Why-logged** | One line: false-positive, false-negative, override, skip, fallback, surprising-good-catch |
| **Notes** | Free text. Include reasoning excerpt or operator-override rationale. Link the commit hash / PR number where relevant. |

---

## Entries

| Date | Trigger | Action | Verdict | Outcome | Why-logged | Notes |
|---|---|---|---|---|---|---|
| 2026-05-03 | 1 | Session 5 — bootstrap commit `bcebe2d` (D1: agent file + `.gitignore` exception) | n/a | proceeded | bootstrap-exception | Per qa-contract §6 — agent file itself cannot be QA'd by an agent that does not yet exist. First commit *after* this routes through the agent. |
| 2026-05-03 | 1 | Session 5 — commit 2 staged (D2a + D2b + D4) attempted | agent-unreachable | aborted (deferred to Session 5.5) | fallback | First fail-closed event of the layer. `Agent type 'esacp-qa' not found` — root cause: in-session `/agents` UI on Claude Code 2.1.126 is read-only of the already-registered set, not a filesystem rescan. Full session restart via `./Cld.sh` required. Per qa-contract §5: trigger 1 fail-open allowed but a downstream push to main (hard-block, fail-closed) made operator-side fail-closed the right call for the whole arc. |
| 2026-05-05 | 1 | Session 9 — doc-sweep commit (Session 9 minutes + Session 10 agenda + this very qa-log row) | approve-with-conditions | proceeded | procedural-condition | Sole condition: append this qa-log row before staging the three files. No substantive finding; logged for completeness. Doc-only, direct-to-main, real-name scan clean (`martinhbramwell` = repo-owner identity; `Logichem` = DB module name faithfully recorded; `hasan` = frozen memory-dir path carve-out). LogiSoluValidations repo creation + push happened in a separate repo and was not part of this ESACP commit. |
| 2026-05-03 | 1 | Session 5.5 Smoke #1 — `git commit -S` of D2a + D2b + D4 (`docs(qa): qa-contract + log seed for esacp-qa subagent (#341)`) | approve | proceeded → `3449a55` | first-verdict | First substantive verdict produced by the layer post-restart. Agent independently read all staged files, cross-checked agent file vs contract, verified relative-link correctness, scanned banned patterns + real names, and surfaced a fifth path (D4-as-stub) the parent missed — correctly rejected it as Path-C-with-extra-steps. Confirmed `docs(qa)` over `feat(qa)` because capability already shipped in `bcebe2d`. |
| 2026-05-03 | 2 | Session 5.5 Smoke #2 — `gh pr merge 342 --merge` to main | approve | proceeded → merge `3f107ef`; #341 auto-closed (`closedAt 2026-05-03T22:44:55Z`) | first-verdict + ruling | First merge verdict. **Notable ruling for future trigger-5 boundary cases**: agent decided the `fixes #341` PR-body auto-close is *subsumed* by this trigger-2 verdict — contract defines triggers by command (not effect); parent is not running `gh issue close`; the auto-close is a server-side consequence of the merge, not a separately executable parent operation; re-invoking after merge would be reactive (close already happened) and a separate `gh issue close 341` would be a no-op. No separate trigger-5 invocation required. |
| 2026-05-03 | 1, 3 | Session 5.5 routine approves: post-restart no-op ping; Smoke #1.5 session-log commit on main (`c6d132e`); `git push origin main` (trigger 3); `git push -u origin feat/esacp-qa-agent` (trigger 3, first-push with upstream tracking) | approve (×4) | proceeded | routine-batch | All trigger 1/3 with no surprise findings. Agent verified GPG signatures (good), divergence (1/0 fast-forward for both pushes), no pre-push hooks, no `.github/workflows/`, no branch protection rules. Smoke #1.5 used `git switch main` to commit Session 5 minutes + Session 5.5 agenda direct-to-main per session-log convention; agent confirmed direct-to-main matches 5/5 recent precedent. |
| 2026-05-04 | 1 | Session 7 — pre-commit on retry PR `ce_sri_svc#3` commit `18f5a58` | approve-with-conditions | proceeded with override | override | QA subagent flagged `Co-Authored-By: Claude Opus 4.7 (1M context)` trailer as model-identity wrong, claiming the active model is `claude-sonnet-4-6`. Subagent had read its own runtime context as the parent's. Parent's system prompt explicitly identifies parent agent as Opus 4.7 (1M), model ID `claude-opus-4-7[1m]`. Override per §2 row 1 (advisory). Trailer kept as-is. Same conflation reappeared at next pre-commit verdict (tcpdump, round 2 approve) — pattern: subagent confuses its own model for parent's. |
| 2026-05-04 | 1 | Session 7 — pre-commit on tcpdump tooling, round 1 (analyze_capture.sh) | reject | revised | surprising-good-catch | QA caught categorical `sed` violation on line 64 of analyze_capture.sh (`sed -n 's/.*IP \([0-9.]\{7,\}\)\.[0-9]\+ > .*/\1/p'`). Parent's deliberation enumerated four architectural paths but did not flag the `sed` use. Bash analyzer scrapped wholesale; replaced with Python `analyze_capture.py` (113 lines, `re` module — explicitly endorsed alternative per CLAUDE.md). All functions decomposed to ≤30 lines. Re-staged + approve at round 2. |
| 2026-05-04 | 3 | Session 7 — pre-push on tcpdump commit `baf4a68` | reject | revised | surprising-good-catch | QA caught real-name nickname "Mighty" in commit body line "controller (Mighty) confirmed SRI emits RST...". Parent's pre-commit deliberation did not include a real-name grep on the message body. Amended commit; first amend used `sed` in the CLI invocation itself (same banned-pattern violation in different shape) and the substitution didn't fire because the pattern didn't account for line breaks. Second amend used Python `str.replace('controller (Mighty)', 'controller')`, succeeded — new SHA `1a7ae58`. Lesson: `sed` ban applies to ad-hoc CLI use during git operations, not just committed scripts. |
| 2026-05-04 | 2 | Session 7 — pre-merge on `ce_sri_svc#2` (squash to main) | reject | overridden-by-operator | override + likely-false-positive | QA flagged `dev01` in `diagnostics/README.md` line 3 as a real-machine-nickname violation of Global CLAUDE.md "no real hostnames in docs" rule. Inconsistent with prior verdicts on the same content: pre-push gates for `b8312ed` and `1a7ae58` both explicitly grepped for `Mighty/toshy/hasan/doowa` and confirmed clean; `dev01` was not flagged there. Operator's read (confirmed): `dev01` is project-convention naming per `hosts_map.yml` ("authoritative host directory — single source of truth"), used universally in commits/issues/session logs (e.g., #343 title literally contains it). Operator: "not a real name. Never exposed to the real world." Override (a). Merge commit `647b0ab`, mergedAt `2026-05-04T20:04:38Z`. **Pattern signal**: subagent inconsistency on what counts as "real name" — early-session strict reading of "Mighty" was correct; late-session strict reading of "dev01" was over-broad. Worth tracking if recurring. |
| 2026-05-04 | 1 | Session 8 — pre-commit on `463b1d3` (#347 audit fix) | approve-with-conditions | revised | surprising-good-catch | QA caught pre-commit ratchet violation parent missed: `core_diff_property.py` 47→50 lines (3-line docstring expansion). Trimmed to 45 before commit; tests still pass. Recurrence pattern (cf. 2026-05-04 commit on `ce_sri_svc#3`): parent under-checks size_baselines.json before staging changes to ratchet-tracked files. New memory `feedback_check_size_baselines_at_commit_time.md` written to address. |
| 2026-05-04 | 1 | Session 8 — pre-commit on `463b1d3` round 2 | reject | revised | routine | Same ratchet, different file: `core_diff_classifier.py` 25→29. Same root cause as round 1. Trimmed to 21. Two iterations on the same anti-pattern in the same commit. Reinforces the new memory above. |
| 2026-05-04 | 3 | Session 8 — pre-push on `463b1d3` (feature branch) | approve | proceeded | routine | All gates pass; feature branch only, blast radius limited. Notable: QA proactively flagged that `core_diff_added_field.py` (62 lines, new) was untracked by pre-commit ratchet — advisory at push gate, became hard condition at PR-merge gate. |
| 2026-05-04 | 3 | Session 8 — pre-commit + pre-push on route_planner `e87a64e` (cross-repo fix) | approve-with-conditions | revised | surprising-good-catch | QA flagged missing acceptance test before push, per `feedback_acceptance_test_required.md`. Parent had only verified the value-shape problem in isolation; no e2e migrate run. Ran G2-clear + bench-migrate on dev02 with new fixtures BEFORE push: exit 0, both Custom Fields verified in DB. Commit message updated to document the local-acceptance evidence + that full e2e provision validation follows post-push (chicken/egg: pipeline clones from remote). Catches a real coverage gap; pattern worth remembering for cross-repo fixes where the remote-pull is part of the test loop. |
| 2026-05-05 | 2 | Session 8 — pre-merge on PR #348 (squash to main) | approve-with-conditions | revised | surprising-good-catch | QA caught that new file `core_diff_added_field.py` (62 lines) was absent from `TARGET_LIMITS` in `pre_commit_size_check.py`. Parent had let the file fall through to the "no limit" branch of `get_limit_for()`, meaning future growth would not be caught by the ratchet. Added `core_diff_added_field.py: 50` to TARGET_LIMITS + trimmed file from 62 to 49 lines (docstring condensation) in follow-up commit `843fb68` on the same branch before merge. Pattern: when adding new files to `customisation_audit/`, audit `TARGET_LIMITS` membership at PR-creation time, not merge time. PR #348 merged `2f64be1` mergedAt `2026-05-05T11:25:17Z`. |
| 2026-05-05 | 1 | Session 10 — pre-commit on `feat(audit):` (LogiSoluValidations sub-branch) | approve-with-conditions | revised | procedural-condition | Sole condition: `hooks_patches_scan.py` docstring claimed a fallback to `PRODUCTION_20260404` if dev02 path was missing, but the fallback was not implemented in `fetch_file()` (silently returns `None`). Removed the false claim from the docstring before the commit landed. Working-tree scan, GPG-sign + Conventional Commits + Co-Authored-By all clean. Cross-repo verdict (LogiSoluValidations is a sibling repo, not ESACP) — agent applied the spirit of the rules correctly. |
| 2026-05-05 | 3 | Session 10 — pre-push attempt 1 (LogiSoluValidations sub-branch, 3 commits) | approve-with-conditions | revised | surprising-good-catch | Two conditions surfaced: (1) `gap_filler_probe.py` at 148 lines is in CLAUDE.md size table's 101+ reject band — operator override on record ("I'll let the overweight file through, for now. We may need new QA rules for this new repo"); ESACP size table is dispatcher/pipeline-targeted, not standalone-probe-script governance. (2) `Logichem` real client name leaked into commit message bodies of `2de9a0f` and `ea2f72e` (working tree had been scrubbed; commit messages had not). The rule's `#239` annotation explicitly scopes commit messages. Remediation: operator-authorised reset-and-recommit — `git reset --soft HEAD~3`, collapsed 3 commits into 2 with clean messages (`feat(audit)` + `docs(readme)`), re-staged. Surprising-good-catch: this caught a precise rule-scope hit the parent had genuinely missed despite a working-tree scan. |
| 2026-05-05 | 3 | Session 10 — pre-push attempt 2 (clean 2-commit branch) | approve | proceeded | (routine-after-remediation) | Both prior conditions resolved — Logichem absent from working tree + reachable commit messages; size override on record. Push proceeded; PR [#1](https://github.com/martinhbramwell/LogiSoluValidations/pull/1) opened to `umbrella/playwright-regression-suite`. |
| 2026-05-05 | 1 | Session 10 — pre-commit on this very doc-sweep commit (Session 10 minutes + Session 11 agenda + this qa-log row) | (filed in this row) | proceeded | procedural-completeness | Doc-only direct-to-main, repo: ESACP. No QA verdict requested specifically for this commit — docs(session-log) scope on direct-to-main is governed by the same advisory-trigger rule as any other commit, but the trigger contract treats commit-1 as advisory; logged here for completeness rather than as a new verdict invocation. Real-name scan: `Logichem` does not appear in the minutes or agenda or this qa-log row (intentional; new repo convention from Session 10). `LogiSolu` and `LogiSoluValidations` appear (deliberate ambiguous form). |
| 2026-05-07 | 1 | Session 12 — pre-commit on doc-sweep (Session 12 minutes + Session 13 agenda + this qa-log row) | approve | proceeded | procedural-completeness + audit-gap-self-caught | Doc-only direct-to-main, repo: ESACP. Parent's close-out audit (forward-tense grep + per-issue findings audit) caught an audit gap *before* invoking QA: the Session 12 retraction of the close-as-won't-fix proposal for #352 was sitting in the draft minutes only, not on the issue itself. Posted #352 comment ([4397298540](https://github.com/martinhbramwell/ESACP/issues/352#issuecomment-4397298540)) before staging. QA subagent verdict was routine-approve given the gap was already closed; the signal here is the close-out audit doing its job (catches what the in-flight audit-step rule prevents from reaching the QA gate). Real-name scan clean (`martinhbramwell` repo-owner identity; `Logichem` absent; `hasan` frozen memory-dir path carve-out). No PRs opened this session — `feedback_pr_merge_before_session_close.md` vacuously satisfied. |
| 2026-05-07 | 1 | Session 12 post-close — pre-commit on follow-up doc commit (revised Session 13 agenda + this qa-log row) covering post-close-out decisions: moot-sweep executed (8 closed, 1 filed), Polvo decided, Phase 4 sequencing revised under no-rework principle, Phases 7+8 added | approve-with-conditions | proceeded | conditions-met + repeat-self-context-confusion | Doc-only follow-up after `1e5d0e9` close-out commit. Conditions on approve: (a) GPG-sign required (met via `-S`), (b) Co-Authored-By trailer required (met). Subagent suggested `Claude Sonnet 4.6` for the Co-Authored-By trailer based on its own runtime context — same pattern logged 2026-05-04 row where subagent confuses its own model for parent's. Parent (Opus 4.7 1M context per system prompt) overrides per documented precedent. Procedural note from subagent: future QA invocations should include verbatim commit command per qa-contract §3; gap noted, not blocking. |
| 2026-05-07 | 1 | Session 12 round-2 close — pre-commit on minutes addendum (Session 12 minutes file gains "Post-close-out addendum" section + this qa-log row) | approve | proceeded | round-2-close-audit-caught | Doc-only direct-to-main, repo: ESACP. Round-2 close-out audit (run after `c7f31c4` follow-up commit landed) caught that the original Session 12 minutes file (in `1e5d0e9`) described several decisions as "pending" when they had subsequently resolved in the post-close segment. "Minutes describe what happened" required updating the minutes file, not just the agenda. Addendum captures: operator-driven scope clarifications, no-rework sequencing principle stated, Polvo decision, Phase 4 sequencing revision, returnable+route_planner refactor confirmed (Phases 7+8), moot-sweep executed (8 closes + #355 filed), revised chronology, memory updates list, QA verdicts on `c7f31c4`. esacp-qa subagent flagged thin enumeration-of-alternatives in the invocation as a procedural note (anti-rubber-stamp mandate per `feedback_enumerate_mechanisms_before_committing.md`); not load-bearing for a doc-only append where the only alternative was "single file with addendum vs separate addendum file". |
| 2026-05-07 | 1 | Session 13 — pre-commit on Phase 1 batch (3 repos: route_planner `b127f2c` fixture+hooks.py, ce_sri `ea8afcc` fixture, ESACP `2c6b580` attribution.yml). Trigger class 1 (advisory). | approve-with-conditions | revised | surprising-good-catch | Substantive condition: route_planner `hooks.py` lacked `fixtures = ["Custom Field"]` declaration — the new fixture file would have been inert during bench migrate. Agent caught it; parent added the line + re-staged before commit. Other anti-rubber-stamp questions (Supplier routing, es.csv strategy, multi-repo orphan risk) ruled acceptable. Three-path enumeration (A: fixture promotion / B: in_core+v14_patch_script / C: 18-entry-strict / D: defer-until-V14-substrate) judged genuine, not strawman. |
| 2026-05-07 | 1 | Session 13 — pre-push on Phase 1 batch (3 sub-branches + ESACP umbrella ref). Trigger class 3 (hard-block). | approve | proceeded | structural-acceptance-deferred | All three pushes to new feature branches (none to main or umbrella/*). Umbrella ref creates topology marker pointing at `main`'s tip — no new commits reach main. Agent flagged that the dev02 acceptance test physically must run post-push (GitHub-source-of-truth constraint per `feedback_respect_original_scripts.md` + "Bespoke App Repos — GitHub is Source of Truth"); issue #356 acceptance criteria explicitly accommodate this sequencing. Real-name scan clean. Path-enumeration (push-now vs manual-copy vs PR-without-push vs ESACP-only-push) judged genuine. Pushes executed: route_planner `phase-1-fixture-equivalent`, ce_sri `phase-1-fixture-equivalent`, ESACP `phase-1-fixture-equivalent` + `umbrella/erpnext-idiomatic-refactor`. |
| 2026-05-07 | 1 | Session 13 close — pre-commit on doc-sweep (Session 13 minutes + Session 14 agenda + this qa-log row + `feedback_check_existing_wip_before_fresh_work.md` + `project_wip_consolidation_plan.md` memory files referenced) | approve-with-conditions | proceeded | session-close-batch + self-referential-row-condition | Doc-only direct-to-main on ESACP. Session 13's strategic finding (wip-branch sprawl already carries Phase 1's intended work; my new branches duplicate it with corrected routing) is documented in minutes + Session 14 agenda + `project_wip_consolidation_plan.md`. The Tracks A/B/C consolidation plan is the durable home; minutes describe what happened (the pivot), not what the original agenda anticipated. Sole QA condition: this very row's verdict cells (originally TBD-pending) had to be populated with the returned verdict before the commit was staged — same self-referential timing pattern as Session 9 row 30. Real-name scan clean (`martinhbramwell` repo-owner identity references in URLs only; `Logichem` absent; `hasan` frozen memory-dir path carve-out). Path enumeration (single-commit / per-file / defer / qa-log-only-defer) judged genuine. Forward-tense audit caught one structural TBD that resolves on this commit. No PRs opened this session — `feedback_pr_merge_before_session_close.md` vacuously satisfied. |
| 2026-05-08 | 1 | Session 14 close — pre-commit on doc-sweep (Session 14 minutes + Session 15 agenda + this qa-log row + `MEMORY.md` index pointer; 8 cross-issue comments posted to #353, #356, #357, #197, #343, #344, #345, #354) | approve-with-conditions | revised | surprising-good-catch + session-close-batch + architectural-pivot + self-referential-row-condition | Doc-only direct-to-main on ESACP. Session 14 pivoted entirely from filing-mode (six original sub-tasks per agenda) to architectural-design-mode through operator-driven Socratic questioning. All six sub-tasks retired without filing; output is the long-term plan (Phases 0–5 + Tracks A–H, breadth-only) captured verbatim in minutes as durable home. Two architectural-decision issues (three-bucket architecture + LogiSoluMemory sibling) deferred to Session 15 Phase 0 completion. **Surprising-good-catch**: parent's pre-commit real-name scan claimed the operating-company name was absent from staged files; QA subagent independently re-scanned the minutes and found 5 occurrences across 4 line-ranges (lines 31, 32, 43, 262, 263) — verbatim operator-quote inside narrative + analytical paraphrase using the real name as a category label + retrospective evidentiary occurrences. Substitution applied at all 5 sites + this very row's earlier draft also carried the literal name (now substituted) before re-staging. Pattern signal: parent's self-checking grep was wrong about its own output; QA caught a precise rule-scope hit despite parent's explicit pre-scan claim. Same shape as row 45 (Session 10 — same-name leak in commit messages caught despite working-tree scan), reinforced. CloudStack dropped from plan (operator decision). Sequential V-ladder cycling on toshy 16GB confirmed. Forward-tense audit: all phrases resolved (some superseded, some captured in plan, some open as Phase 1 prerequisites — listed in minutes "Operator decisions captured" table). 8 issue comments posted before commit per close-out audit Step 2. No PRs opened — `feedback_pr_merge_before_session_close.md` vacuously satisfied. Path enumeration (single-file minutes vs split-by-decision; full-plan-inline vs plan-as-separate-memory-file; comment-then-commit vs commit-then-comment ordering) judged genuine. Self-referential row pattern: verdict cells filled in after `esacp-qa` invocation + after substitution remediation, before staging. |
| 2026-05-08 | 1 | Session 15 close — pre-commit on doc-sweep (Session 15 minutes + Session 16 agenda + this qa-log row; 3 architectural-decision issues filed earlier in session: #358 three-bucket, #359 LogiSoluMemory, #360 Track H seedling; 8 follow-up comments posted on #353, #356, #357, #197, #343, #344, #345, #354) | approve | proceeded with re-invocation | surprising-good-catch (inverted) + session-close-batch + self-referential-row-condition | Doc-only direct-to-main on ESACP. Session 15 was Phase 0 completion per Session 14's plan; filing-only governance, no architectural drama. **Surprising-good-catch (inverted)**: QA's first verdict (`a8584d382b89579fb`) was approve-with-conditions on a false-positive Condition 1 — claimed open-issue count `36` in minutes/agenda mismatched live `30`. Both parent (`gh issue list … --limit 50 \| wc -l`) and re-rule QA (`gh issue list … --limit 100 --json number --jq 'length'`) authoritatively confirmed `36`. The first agent's `30` was the GitHub CLI's silent default-limit-30 truncation. Parent re-invoked with JSON-count evidence; second QA agent (`a8adcb2f556c1439e`) independently confirmed 36 and lifted the condition: final verdict approve. Pattern signal: `gh issue list` default-30 trap can trip both parent AND QA — worth memory'ing if it recurs (this is first observed instance). Same self-referential row pattern as Sessions 9/13/14 — verdict cells populated after final approve before staging. `toshy` ruling reaffirmed (project-convention DNS alias from `hosts_map.yml`, dev01-precedent class). Forward-tense audit complete (minutes' own audit section maps every claim to durable home: issue checklists for closure-expected statements, next-agenda for next-session claims). Real-name scan clean (`martinhbramwell` repo-owner identity in `gh` command lines only; no operating-company-name leakage). 1:1:1 vacuously satisfied (governance work, not substantive code). No PRs opened — `feedback_pr_merge_before_session_close.md` vacuously satisfied. Path enumeration (direct-to-main / branch+PR / bundle-into-later-commit) judged genuine. |
| 2026-05-08 | 1 | Session 16 close — pre-commit on doc-sweep (Session 16 minutes + Session 17 agenda + this qa-log row; #359 audit-completion comment posted earlier in session at [4409884536](https://github.com/martinhbramwell/ESACP/issues/359#issuecomment-4409884536)). Session was Phase 1 first move per #359 closure checklist: real-name audit on the existing memory directory at `/home/hasan/.claude/projects/-home-hasan-projects-Logichem-ESACP/memory/` (5 files / 11 occurrences scrubbed; 0 filename leaks; final re-grep clean). Memory directory itself remains uncommitted per operator decision #7 (LogiSoluMemory standup in Session 17 will capture the post-scrub baseline). | approve | proceeded | session-close-batch + self-referential-row-condition | Doc-only direct-to-main on ESACP. Session 16 was scoped audit work; ran exactly to plan. Item-group product-line observation flagged-and-dropped per operator (durable home: minutes "Operator decisions captured" row 6). Real-name scan on staged files: QA independently confirmed all `Logichem` occurrences are (a) frozen encoded-path carve-out (`-home-hasan-projects-Logichem-ESACP`, `$BESPOKE_ROOT/PRODUCTION_20260404/`), (b) operationally-necessary grep-token quotations in audit records, or (c) before/after scrub-diff evidence in the minutes' diff section — none are narrative identity leaks. Product names (`Matrix Clean, Biodox, Oxycal, Aguas y Minerales`) appear once in operator-decisions row 6 documenting the operator's decision to decline extended scope; QA judged this evidentiary/governance use, not disclosure — operator's durable-home decision governs. `hasan` username appears in encoded memory-dir path only (same frozen carve-out class). Forward-tense audit complete (close-out audit map: every "I'll" / "should" / "next" phrase resolved either as completed tool call or operator-instructed drop; durable homes are #359 comment + this minutes file). #359 comment posted before declaring DONE per close-out audit Step 2. No PRs opened this session — `feedback_pr_merge_before_session_close.md` vacuously satisfied. 1:1:1 vacuously satisfied (audit work, not substantive code). Path enumeration (single doc-sweep commit / split-by-file / batched-with-future-doc-commit / minutes-only-defer-rest) judged genuine; QA confirmed structural rejection reasons (not strawman). Self-referential row pattern as Sessions 9/13/14/15: verdict cells populated after `esacp-qa` invocation before staging. Agent ID: `acacee40bcd368326`. |
| 2026-05-08 | 3 | Session 17 — pre-push on first-push of `martinhbramwell/LogiSoluMemory:main` (seed commit `f7138d1`: 130 files / 5128 insertions = 1 new README + 129 pre-existing memory files; `git remote add origin git@github.com:martinhbramwell/LogiSoluMemory.git && git push -u origin main`). | approve | proceeded | substantive-good-catch + first-push-fresh-private-repo | Hard-block trigger 3. QA invocation `a40c5b80ca8303339`. Independently re-verified: `grep -rli 'logichem'` returns 0 files (Session 16 scrub state still clean); GPG signature good; remote pre-push state confirmed empty/private; commit metadata compliant; Co-Authored-By trailer matches harness directive (`Opus 4.7`). Path enumeration A/B/C (init-elsewhere-then-copy / init-in-place-then-mv / single-shot-with-source-and-push) judged genuine — Path B chosen (single working tree throughout, `.git/` travels with `mv`). **Substantive non-blocking flag**: machine-name residual concern raised — `Mighty`, `toshy`, `iridium.blue` appear in several memory files; in scope of Phase 1 memory rewrite (Sessions 18–25 backlog), governed for this session by Session 16 decision #1 (audit anchor = `logichem` only) and the private-repo posture. QA explicitly framed this as future Phase-1 work, not a session-17 blocker. Worth tracking if recurrence rate of "audit-scope was narrower than rules" becomes a pattern. |
| 2026-05-08 | 3 | Session 17 — pre-push on follow-up commit `951e8e4` to LogiSoluMemory:main (MEMORY.md index update only: 7 ins / 4 del adding `## Memory home` section + weaving `LogiSoluMemory, LogiSoluValidations` into `$BESPOKE_ROOT` example list). | approve | proceeded | routine-after-seed + cosmetic-trailer-note | Hard-block trigger 3. QA invocation `adb4c9002ddd792d3`. Diff verified independently; fast-forward of one tiny doc commit; mission-aligned (closes #359 checklist item 5). Path enumeration A/B/C (concise-pointer-here / new-dedicated-topic-file / fold-into-existing-section) judged genuine. **Cosmetic note (recurring observation)**: Co-Authored-By trailer says `Claude Opus 4.7` where stale ESACP `CLAUDE.md` template still says `Claude Opus 4.6`; harness directive's `Opus 4.7` overrides — same model-version trailer pattern logged earlier (rows 35, 49). QA judged not-blocking for an auxiliary doc commit. Action item for Phase 1 memory/CLAUDE.md rewrite: bring CLAUDE.md template into alignment with harness directive. |
| 2026-05-08 | 1 | Session 17 close — pre-commit on doc-sweep (Session 17 minutes + Session 18 agenda + this qa-log row + `CLAUDE.md` minimal addition declaring LogiSoluMemory as behavioral-memory home). Session was Phase 1 LogiSoluMemory standup per #359 closure checklist (items 2–6 of 6; item 1 closed in Session 16). All six agenda sub-tasks executed: empty-repo create, `git init`+seed commit, first push, `mv`+symlink swap, MEMORY.md index update with second push, CLAUDE.md root edit (minimal — broader three-bucket discipline rewrite scoped to later Phase 1 sessions per agenda's deferral allowance + operator's "do all six this session" override). | approve | proceeded | session-close-batch + self-referential-row-condition | Doc-only direct-to-main on ESACP. Session 17 ran cleanly through agenda sub-tasks 1–6 with no reframe, no remediation rounds. Two mid-session QA verdicts (rows above) both approve on first invocation. QA invocation `aa4777a10d5a7e673`. CLAUDE.md edit is narrowly-scoped (one paragraph after Mission line) — full three-bucket discipline rewrite (replacing "Bespoke App Repos — GitHub is Source of Truth" section) deferred to Sessions 18–25 per agenda's Phase 1 sequencing. `size_baselines.json` does not track `CLAUDE.md` (ratchet is dispatcher/pipeline-targeted) — addition does not trip any size gate. QA explicitly accepted Trigger 1 verdict as effectively gating Trigger 3 (immediate push) per established convention rows 53–56; no separate hard-block invocation requested. **Trigger 5 #359 close verdict** captured in next row below (line 60), pulled forward into Session 17 by close-out-audit follow-up commit per audit Step 2 finding (was originally deferred to Session 18 qa-log append). Verdict transcript also in #359 closing comment [4409929832](https://github.com/martinhbramwell/ESACP/issues/359#issuecomment-4409929832); closing-commit references `f7138d1`, `951e8e4`, `1bfa02d`. Self-referential row pattern as Sessions 9/13/14/15/16. QA noted `hasan` username appears in command transcripts (lines 87–90 of minutes) — judged consistent with established frozen-carve-out class for evidentiary command transcripts (Session 14/16 precedent), not a narrative-identity violation. Co-Authored-By model trailer (`Opus 4.7`) overrides stale CLAUDE.md template (`Opus 4.6`) per harness directive — recurring observation also logged rows 35, 49. No PRs opened — `feedback_pr_merge_before_session_close.md` vacuously satisfied. 1:1:1 housekeeping bundle (CLAUDE.md edit + session-log artifacts + qa-log; the cross-repo memory-standup work is on LogiSoluMemory, not part of this ESACP commit). Path enumeration (single doc-sweep commit / split-by-file / defer-CLAUDE.md-edit-to-Phase-1-rewrite / commit-now-issue-close-later) judged genuine. |
| 2026-05-08 | 5 | Session 17 — pre-`gh issue close 359 --reason completed --comment "..."` (institutional-memory mutation; closing #359 LogiSoluMemory standup decision after Session 17 sub-tasks 2–6 of 6 landed; closing-commit hashes `f7138d1`, `951e8e4`, `1bfa02d`). Recorded in this session via close-out-audit follow-up commit (the trigger-5 verdict was obtained AFTER the session-close commit `1bfa02d` landed; durable home for the verdict transcript is the closing comment on #359 [4409929832](https://github.com/martinhbramwell/ESACP/issues/359#issuecomment-4409929832), and this row is the secondary record per qa-log convention). | approve-with-conditions | proceeded with condition discharged | substantive-good-catch + acceptance-checklist-conjunction-violation | Hard-block trigger 5. QA invocation `a8b3b96353dab8fc0`. **Substantive condition**: parent's draft closing comment buried the item-5 partial-scope note (the `platforms/kvm/session_start.py` half of #359's checklist item 5 was deferred to #358 by operator scope decision). QA observed that closing against a partially-unsatisfied conjunction in the acceptance checklist conflicts with `feedback_acceptance_test_required.md` — but the partial close is defensible because #358 is open and covers the remainder via its checklist item 5 (session-start protocol extension), and the symlink mechanism makes the script's runtime path-lookup transparent. Condition: surface the partial-scope note at the TOP of the closing comment (first/second paragraph), not buried at the end, so future readers see deferred scope before the list of completions. **Discharged before close ran**: closing comment was restructured to lead with "Scope note up front — item 5 is partially deferred." Path enumeration (close-now / keep-open / new-sub-issue / scope-amendment-comment-then-close) judged genuine; QA noted Path C (sub-issue) was dismissed too quickly as "procedural overhead" but the chosen Path A with explicit scope-amendment satisfied the institutional-memory concern. Audit step 2 follow-up: companion comment posted to #358 ([4410746487](https://github.com/martinhbramwell/ESACP/issues/358#issuecomment-4410746487)) capturing the new finding (deferred-from-#359 work scoped within #358's checklist item 5). Issue closed: `2026-05-08T23:41:40Z`, reason=completed. |
| 2026-05-09 | 3 | Session 18 — pre-push on first-push of `martinhbramwell/LogiSoluKnowBase:main` (seed commit `a8995e1`: 1 file / 91 insertions = 1 new README; `git remote add origin git@github.com:martinhbramwell/LogiSoluKnowBase.git && git push -u origin main`). Bucket-2 repo standup per #358 closure-checklist item 1. | approve | proceeded | first-push-fresh-private-repo + scope-reframe-validation | Hard-block trigger 3. QA invocation `aba8514fa4a12900f`. Independently re-verified: README scope claims match #358 verbatim (bucket placement, "what this repo holds / does NOT hold"); `2c6b580` confirmed via `git show --stat` to exclusively modify `config/customisation_attribution.yml` (validates the scope-reframe rationale that audit-framework content is bucket-1 and stays on ESACP); GPG signature good; Co-Authored-By trailer matches harness directive (`Opus 4.7`). **Anti-rubber-stamp scope-reframe validation**: parent reframed agenda sub-tasks 2/3 (umbrella migration) to no-ops on bucket-boundary grounds; QA explicitly verified the boundary analysis is "not post-rationalisation — the boundary analysis is correct". Path enumeration for the push itself was thin-but-defensible (single root commit to empty remote — A: push now / B: defer / C: different remote — only A serves the objective). Real-name scan clean. Recurring Co-Authored-By model trailer note re-flagged (rows 35, 49, 56, 58); not blocking. |
| 2026-05-09 | 3 | Session 18 — pre-push on follow-up commit `ae7166a` to LogiSoluMemory:main (MEMORY.md index update only: 8 ins / 4 del adding `## KnowBase home` section + weaving `LogiSoluKnowBase` into `$BESPOKE_ROOT` example list + paragraph re-flow). | approve | proceeded | routine-after-seed + line-count-debt-reaffirmed | Hard-block trigger 3. QA invocation `a02cb55e58c781259`. Diff verified independently (12 lines changed, MEMORY.md only); fast-forward of one tiny doc commit; mission-aligned (advances #358 closure-checklist via index-pointer registration). Path enumeration A/B/C (concise-pointer-here / new-dedicated-topic-file / fold-into-existing-section) judged genuine. **"No umbrella branch yet" claim** independently verified locally (`git branch --list 'umbrella/*'` empty on LogiSoluKnowBase). MEMORY.md line-count debt acknowledged (210 → 214; 200-line ceiling already exceeded pre-Session-17, Phase 1 trim work). Recurring Co-Authored-By model trailer note re-flagged (rows 35, 49, 56, 58). Same shape as Session 17 row 58 (LogiSoluMemory index-pointer push). |
| 2026-05-09 | 1 | Session 18 close — pre-commit on doc-sweep (Session 18 minutes + Session 19 agenda + this qa-log row + 3 Session-18 rows above). Session was Phase 1 LogiSoluKnowBase standup per #358 closure-checklist item 1; agenda sub-tasks 2/3 (umbrella migration) reframed to no-ops on bucket-boundary grounds (operator-approved mid-session); ladder-fixture orphan branch filed as #361 hygiene issue. | approve-with-conditions | proceeded with conditions discharged | session-close-batch + self-referential-row-condition + scope-reframe + trailer-condition | Doc-only direct-to-main on ESACP. QA invocation `ade9be606c94f7934`. Session 18 included one substantive scope reframe (sub-tasks 2/3 collapsed to no-ops; behavioral-theatre acknowledged in minutes "Behavioral signal" section). Two mid-session QA verdicts (rows above) both approve on first invocation. **Behavioral signal logged**: parent initially presented the scope-reframe as a four-option decision menu ("Path D"); operator pushback exposed the engineering-theatre pattern; revised to advise+proceed. Action item: file `feedback_no_decision_theatre_on_clerical_work.md` in LogiSoluMemory after this commit. **Two procedural conditions**: (1) Co-Authored-By trailer absent from the commit-command form shown in invocation (`-m` single-line form); discharged by using heredoc with full trailer (`Claude Opus 4.7 (1M context)`); QA verified the trailer is mandatory per CLAUDE.md and was load-bearing as a procedural check. (2) Self-referential row TBD cells populated before staging per established pattern (Sessions 9/13/14/15/16/17); discharged before staging. Independently re-verified by QA: real-name occurrences in minutes are evidentiary command transcripts (frozen carve-out class, precedent rows 57/59) or `operating-company-specific` placeholder form; machine names in agenda are meta-documentation of carried-forward deferred Phase-1 work item (precedent row 57); open-issue count 36 confirmed via `--limit 50 --json number --jq 'length'` (Session 15 row 55 trap avoided); #361 filed; #358 checklist item 1 satisfied; staged files are doc-only ESACP; no PRs opened — `feedback_pr_merge_before_session_close.md` vacuously satisfied. 1:1:1 housekeeping bundle (session-log artifacts + qa-log; the cross-repo standup work is on LogiSoluKnowBase + LogiSoluMemory, not part of this ESACP commit). Path enumeration (single doc-sweep commit / split-by-file / defer-behavioral-memory-to-Session-19) judged genuine. Self-referential row pattern as Sessions 9/13/14/15/16/17. |

---

## Session-close batching protocol

At each session close (when filing minutes):

1. Review every `Agent(subagent_type: "esacp-qa", …)` invocation in the session.
2. Identify the ones with signal per the "Why-logged" column legend (skip routine approves with no surprise).
3. Append rows for each in this table.
4. If verdict count for the session is high enough that the table feels noisy, summarise: one row noting `N routine approves` + individual rows for everything notable.
5. Commit alongside session minutes (`docs(session-log)` scope is fine; no separate `docs(qa-log)` commit unless the log update is the only change).

---

## Cross-references

- Trigger contract: [`qa-contract.md`](qa-contract.md)
- Agent definition: [`../.claude/agents/esacp-qa.md`](../.claude/agents/esacp-qa.md)
- Issue: [#341](https://github.com/martinhbramwell/ESACP/issues/341)
- Originating memory rule: `feedback_enumerate_mechanisms_before_committing.md` (memory dir, not in repo)
